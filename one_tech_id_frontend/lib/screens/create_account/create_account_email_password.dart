import 'package:flutter/material.dart';

class CreateAccountEmailPassword extends StatelessWidget {
  const CreateAccountEmailPassword({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SizedBox.shrink(),
    );
  }
}
